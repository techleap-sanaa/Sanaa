import { configureStore } from "@reduxjs/toolkit";

export const store = configureStore({
  reducer: {
    // Hey again, here we're going to add our reducers to store our states once we start building them
  },
});


